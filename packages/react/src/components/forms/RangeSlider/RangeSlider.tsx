import React from 'react';

export interface LumaRangeSliderProps {
  children?: React.ReactNode;
}

export const LumaRangeSlider: React.FC<LumaRangeSliderProps> = ({ children }) => {
  return <div>{children}</div>;
};
